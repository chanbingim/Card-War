using GamePlay.Enum;
using System;

namespace UI.Enum
{
    public enum UIID
    {
        None,
        Inventory,
        Shop,
        Option,
        Fade,
        CashShop,
        CharacterInfo
    }

    public enum EUICanvas
    {
        Screen,
        Screen_Popup,
        World,
        Overlay,
        END,
    }
}

